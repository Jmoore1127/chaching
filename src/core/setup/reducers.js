import {combineReducers} from 'redux';
import {reducer as routes} from "./router.reducer";

export const rootReducer = combineReducers({
    // app: combineReducers({
    //
    // }),
    routes
});
